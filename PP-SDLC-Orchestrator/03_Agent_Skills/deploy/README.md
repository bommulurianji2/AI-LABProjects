# Deploy Agent — Not Yet Implemented

This agent is defined in the requirement baselines (see `docs/requirements_history/`) but has no manifest or runtime yet.

To implement: add `manifest.yaml` + `SKILL.md` here, an adapter under `backend/app/adapters/`, register its output artefact type, and add tests. See `03_Agent_Skills/orchestrator/` and `03_Agent_Skills/analysis/` for the reference pattern built in session 1.
